# PostgreSQL 数据库密码（pg 容器与 backend 连库共用）
# 部署时请复制本文件为 .env 并填入真实密码：cp .env.example .env
POSTGRES_PASSWORD=CHANGE_ME

# 后端 JWT 签名密钥，覆盖 config.yaml.example 里的 CHANGE_ME 占位符。
# 请用强随机值，例如：openssl rand -hex 32
AUTH_JWT_SECRET=CHANGE_ME_REPLACE_WITH_STRONG_RANDOM
