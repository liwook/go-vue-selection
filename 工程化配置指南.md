# 工程化工具链部署指南

本指南记录本项目（Vue 3 + Vite + TypeScript 前端 + Go 后端 monorepo）所集成的工程化工具链，各工具的服务范围与职责见下表（细节见第 3～8 节）：

**当前启用状态**：

| 工具 | 状态 | 服务范围 | 职责 / 备注 |
| --- | --- | --- | --- |
| Biome | ✅ 启用 | 前端 | lint + 格式化主力，覆盖 `.js/.ts` 及 `.vue` 的 `<script>` 块 |
| ESLint | ✅ 启用 | 前端 | 仅补 `.vue` 的 `<template>`（外层 `vue-eslint-parser` + 内层 `@typescript-eslint/parser` 解析脚本块，详见第 4 节） |
| Stylelint | ❌ 禁用 | 前端 | 待 SCSS / 复杂样式需求时启用（见第 5 节） |
| golangci-lint | ✅ 启用 | Go 后端 | 后端聚合 lint（见第 8 节） |
| markdownlint-cli2 | ✅ 启用 | 前后端共用 | Markdown 格式化（纯 JS，天然支持中文路径，见第 8.4 节） |
| Lefthook | ✅ 启用 | 前后端共用 | Git 钩子调度，统一编排上述所有检查 |
| Commitlint | ✅ 启用 | 前后端共用 | 校验提交信息格式（Conventional Commits） |
| pnpm | ✅ 启用 | 前后端共用 | 强制包管理器约束（见第 2 节），本身非检查工具 |

### 0. 目录布局（monorepo，从一开始即规划好）

本项目是 pnpm workspace monorepo，**前端与后端从项目初始化起就是独立子包**，不在仓库根目录堆放业务代码：

```
.
├─ README.md               # 已存在（勿删）
├─ .gitignore              # 已存在（勿删，Biome 通过 vcs.useIgnoreFile 复用它，前端无需单独 ignore 文件）
├─ package.json            # 根：pnpm workspace 配置 + 跨端共用脚本（prepare）
├─ pnpm-workspace.yaml     # 声明 workspace 子包（apps/*）
├─ .npmrc                  # 强制使用 pnpm + 自动安装 peer 依赖（见第 2 节）
├─ lefthook.yml            # 跨端共用：Git 钩子调度（根级，仅此一份）
├─ commitlint.config.mjs   # 跨端共用：提交信息校验（根级，使用 ESM 后缀）
├─ .gitattributes          # 统一换行符为 LF（配合 Biome 的 lineEnding: lf，见下）
├─ apps/
│  ├─ web/                 # 前端子包（Vue 3 + Vite + TS）
│  │  ├─ package.json      # 前端依赖与脚本（dev / build / lint / format）
│  │  ├─ biome.json        # 前端 lint + 格式化配置（随前端子包，主力工具）
│  │  ├─ eslint.config.js  # 仅处理 .vue 的 <template>（随前端子包，最小集）
│  │  ├─ src/  vite.config.ts  ...
│  └─ server/              # Go 后端子包
│     ├─ go.mod  main.go  cmd/  internal/
│     └─ .golangci.yml     # Go 聚合 lint 配置（随后端子包）
```

**配置与执行目录铁律**（具体文件落位见上方目录树）：

- **根目录只放跨端共用的东西**：pnpm workspace、`lefthook.yml`、根 `package.json`、`commitlint.config.mjs`、`.npmrc`，以及本指南文档。`README.md` / `.gitignore` 是已存在的仓库文件，不要改动。
- **命令须在对应子包目录内执行**：前端 `cd apps/web` 后跑 `biome` / `eslint` 等；后端 `cd apps/server` 后跑 `gofmt` / `go vet` / `golangci-lint`。即"配置随子包走，命令也随子包走"，不要从仓库根直接对子包文件执行工具。

---

## 目录

1. [整体架构与协作关系](#1-整体架构与协作关系)
2. [强制使用 pnpm](#2-强制使用-pnpm)
3. [ESLint（仅 `.vue` 模板检查，前端最小集）](#3-eslint仅-vue-模板检查前端最小集)
4. [Biome（lint + 格式化主力，仅前端）](#4-biomelint--格式化主力仅前端)
5. [Stylelint（样式检查，暂未安装，后续按需启用）](#5-stylelint样式检查暂未安装后续按需启用)
6. [Commitlint（提交信息规范，前后端共用）](#6-commitlint提交信息规范前后端共用)
7. [lefthook（Git 钩子自动化，前后端共用）](#7-lefthookgit-钩子自动化前后端共用)
8. [Go 后端工程化（仅 Go 后端）](#8-go-后端工程化仅-go-后端)
   - [8.1 目录结构](#81-目录结构)
   - [8.2 golangci-lint（聚合式 lint）](#82-golangci-lint聚合式-lint)
9. [从零初始化 monorepo 骨架](#9-从零初始化-monorepo-骨架)
   - [9.0 从零初始化 monorepo 骨架（首次必读）](#90-从零初始化-monorepo-骨架首次必读)
   - [9.1 仓库根 `package.json`（workspace 根）](#91-仓库根-packagejsonworkspace-根)
   - [9.2 前端子包 `apps/web/package.json`](#92-前端子包-appswebpackagejson)
   - [9.3 从根目录调用前端脚本](#93-从根目录调用前端脚本)

---

## 1. 整体架构与协作关系

各工具的协作链路如下（`git commit` 时由 lefthook 统一调度，commitlint 校验提交信息）：

```
git commit
   │
   ├─ pre-commit 钩子 ──► lefthook
   │                        ├─ *.vue 的 <template> ──► eslint --fix（仅 vue/* 规则，vue-eslint-parser）
   │                        ├─ *.js / *.ts / *.vue(<script>) / *.json / *.css ──► biome check --write（lint + 格式化）
   │                        └─ *.go              ──► gofmt -w / go vet
   │                        （stylelint 暂未启用，后续有 CSS 规范需求时再接入）
   │
   └─ commit-msg 钩子 ──► commitlint（校验提交信息格式）
```

---

## 2. 强制使用 pnpm

项目通过**三重保险**强制使用 pnpm，以下配置均写在**仓库根目录**的 `package.json`（即 pnpm workspace 根）中：

### 2.0 安装 pnpm

> 这是**环境准备**步骤，和仓库骨架无关——直接装最新稳定版即可，无需读取或依赖任何仓库文件。

安装 **pnpm 最新稳定版**：

```bash
npm install -g pnpm            # 用 npm 全局安装（仅这一次允许用 npm）
```

安装后确认：

```bash
pnpm -v    # 输出版本号即成功
```

> ⚠️ 仅在**安装 pnpm 这一步**允许使用 `npm`（即 `npm install -g pnpm`）；之后所有项目依赖安装都必须用 `pnpm install`，否则会被根 `.npmrc` 的 `engine-strict=true` + `engines.pnpm` 拦截（见 2.1 / 2.3）。

### 2.1 强制约束配置（根目录）

在**仓库根目录**放两个文件即可锁定 pnpm：

```ini
# 仓库根/.npmrc
engine-strict=true        # 启用 engines 强校验，否则第 2 条只警告不拦截
auto-install-peers=true   # 自动补 vue / eslint 等 peer 依赖，省去手动补装
```

```json
// 仓库根/package.json
{
  "engines": { "pnpm": ">=9" },              // 最低版本；需配合上方 engine-strict 才报错拦截
  "packageManager": "pnpm@<版本号>"          // 可选：Corepack 锁定统一版本，填 pnpm -v 所见；不填也能用
}
```

效果与特点：

- 用 `npm` / `yarn` 安装、或 pnpm 版本过低时，安装**直接报错退出**。
- 声明式、零脚本、跨平台，比手写 `preinstall` 脚本更稳，是 pnpm 官方推荐做法。

> ✅ **第 2 节到此结束。** 接下来按【第 9.0 节】「步骤 1 → 6」搭建骨架；第 3～8 节是各工具详细配置，搭建时按需回查。

---

## 3. Biome（lint + 格式化主力，仅前端）

> 配置文件 `biome.json` 写在 **`apps/web/`**，命令（`pnpm lint` / `pnpm format` / `biome ...`）在该目录内执行。Biome 是 Rust 实现的一体化工具，负责**前端格式化 + 所有 `.js/.ts` 以及 `.vue` 里 `<script>` 块的语义 lint**。

### 3.1 演进：以前的做法 vs 现在现代化做法

| | 以前（Prettier + ESLint 双工具） | 现在（Biome 为主 + ESLint 仅补位） |
| --- | --- | --- |
| 格式化 | Prettier 单独跑 | Biome 内置格式化，一条命令搞定 |
| JS/TS lint | ESLint + `@typescript-eslint` 规则集 | Biome `recommended` 规则集（Rust 实现，秒级） |
| `.vue` 模板 | ESLint + `eslint-plugin-vue` 全量接管 | ESLint **仅**查 `<template>` 模板 |
| 工具数量 | 2 个进程、2 套配置、启动慢 | 1 个主力（Biome）+ 1 个极简补位（ESLint） |
| 痛点 | 两套规则易冲突、配置冗余、CI 慢 | 主力统一管代码，ESLint 只补 Biome 看不懂的模板 |

**结论**：Biome 已覆盖前端 90% 的 lint + 全部格式化需求，唯一它**做不到**的是解析 Vue 的 `<template>`（模板语法它看不懂）。这部分才引入 ESLint 补位——见第 4 节。

### 3.2 安装依赖

在 `apps/web/` 内安装（Biome v2 包名 `@biomejs/biome`，CLI 为 `biome`）：

```bash
pnpm add -D @biomejs/biome
```

### 3.3 配置文件 `biome.json`

开启 `recommended` 规则集（Biome v2 用 `preset` 字段），格式化风格对齐旧 Prettier（无分号、单引号、100 宽），并对 `.vue` 关闭与 vue 模板重复的格式化项避免双跑冲突：

```json
{
  "$schema": "https://biomejs.dev/schemas/2.5.7/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignoreUnknown": true
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100,
    "lineEnding": "lf"
  },
  "javascript": {
    "formatter": {
      "semicolons": "asNeeded",
      "quoteStyle": "single",
      "trailingCommas": "all"
    }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended"
    }
  },
  "overrides": [
    {
      // .vue：Biome 不解析 <template>，会把模板实际用到的组件误报为 unused；
      // useImportType 也会被误判。关闭后交给 ESLint 判定，.js/.ts 仍由 Biome 检查。
      "includes": ["**/*.vue"],
      "linter": {
        "rules": {
          "style": {
            "useImportType": "off"
          },
          "correctness": {
            "noUnusedImports": "off",
            "noUnusedVariables": "off"
          }
        }
      }
    },
    {
      // 独立 .svg（favicon / 雪碧图 symbol）无需无障碍 <title>，关掉不影响 .vue 内联 svg
      "includes": ["**/*.svg"],
      "linter": {
        "rules": {
          "a11y": {
            "noSvgWithoutTitle": "off"
          }
        }
      }
    }
  ]
}
```

要点：

- `$schema` 须与本机 `@biomejs/biome` 版本一致，升级后用 `pnpm --filter web exec biome --version` 查当前版本。
- 格式化风格见上方配置：`asNeeded` 分号、`single` 单引号、`lineWidth: 100`、`lf` 换行。
- `overrides` 的关闭理由见配置注释（`.vue` 避免模板误报交给 ESLint；`.svg` 独立图标无需无障碍 `<title>`）。

### 3.4 运行

```bash
cd apps/web
pnpm format                 # biome check --write .（lint 自动修复 + 格式化）
pnpm exec biome check .     # 仅检查，不修改
pnpm exec biome format .    # 仅格式化
```

> `biome check --write` 一条命令同时完成「lint 修复 + 格式化」，是日常首选。

---

## 4. ESLint（补位：仅 `.vue` 模板，最小集）

承接第 3 节：Biome 看不懂 `.vue` 的 `<template>`，但模板里的 vue 规则（组件名、自闭合、属性换行等）仍要查——这就是 ESLint 唯一存在的原因。它**只查模板，不 lint TS、不格式化**（那些交给 Biome），所以依赖极简，**不引入** `@eslint/js` / `typescript-eslint`(规则集) / `eslint-config-prettier`。

> 配置文件 `eslint.config.js` 写在 `apps/web/`，命令在该目录内执行（如 `pnpm --filter web lint:vue`）。

### 4.1 安装依赖

在 `apps/web/` 内安装（或任意目录用 `pnpm --filter web add -D ...`）：

```bash
cd apps/web
pnpm add -D eslint eslint-plugin-vue vue-eslint-parser @typescript-eslint/parser globals
```

| 包名                    | 作用                                                                              |
| ----------------------- | --------------------------------------------------------------------------------- |
| `eslint`                | ESLint 核心（Flat Config，`eslint.config.js`）                                    |
| `eslint-plugin-vue`     | Vue SFC 规则，提供 `flat/recommended` 配置                                        |
| `vue-eslint-parser`     | 解析 `.vue` 的 `<template>`/`<script>`/`<style>` 三块（由 `eslint-plugin-vue` 依赖并自动启用） |
| `@typescript-eslint/parser` | 仅作**内层 TS 解析器**：`vue-eslint-parser` 不认识 `<script lang="ts">` 里的 TS 语法，必须挂它来解析脚本块，否则报 `Parsing error`。**只借 parser、不引入 `typescript-eslint` 规则集**，故不与 Biome 冲突 |
| `globals`               | 提供 `browser` / `node` 等全局变量定义                                           |

### 4.2 配置文件 `eslint.config.js`（Flat Config，仅 `.vue`）

`export default` 导出配置对象数组，**只针对 `.vue` 启用 `eslint-plugin-vue` 的模板规则**，`.js/.ts` 交给 Biome：

```js
import pluginVue from 'eslint-plugin-vue'
import globals from 'globals'

const vueRecommended = pluginVue.configs['flat/recommended']

export default [
  {
    ignores: ['dist', 'node_modules', 'public', 'coverage'],
  },
  {
    files: ['**/*.vue'],
    languageOptions: {
      globals: { ...globals.browser },
      parser: pluginVue.parser, // vue-eslint-parser：负责拆出 <template>/<script>/<style>
      parserOptions: {
        // 内层 TS 解析器：vue-eslint-parser 不认识 <script lang="ts"> 里的 TS 语法，
        // 必须挂 @typescript-eslint/parser 来解析脚本块，否则报 Parsing error。
        // 注意：只借它的 parser，不引入 @typescript-eslint 的规则集（eslint-plugin），
        // 因此不会与 Biome 在 .ts 规则上重叠冲突。
        parser: '@typescript-eslint/parser',
      },
    },
  },
  ...(Array.isArray(vueRecommended) ? vueRecommended : [vueRecommended]),
  {
    rules: {
      'vue/multi-word-component-names': 'off', // 允许 App.vue 单字组件名
      'vue/html-self-closing': 'off',           // 关闭过时 Vue 2 风格的自闭合约束
      'vue/max-attributes-per-line': 'off',     // 关闭每行属性数限制，交给 Biome 管格式
    },
  },
]
```

分工一句话：ESLint「看模板」（含把 `<script>` 块解析出来交给 Biome），Biome「管代码（含 `.vue` 里的 `<script>` 的 TS lint + 格式化）」。

### 4.3 运行

```bash
cd apps/web
pnpm lint:vue              # 只检查 .vue 模板（= eslint .）
pnpm exec eslint src --fix # 对 src 下 .vue 的模板自动修复
```

---

## 5. Stylelint（样式检查，暂未安装，后续按需启用）

> 当前暂未安装 Stylelint：无明确 CSS/SCSS 规范需求前不引入，避免 lefthook 里 `pnpm exec stylelint` 找不到 bin 报错。后续有需求时按下面补装，lefthook 已预留注释态命令。

### 5.1 安装依赖（启用时执行）

```bash
pnpm add -D stylelint stylelint-config-standard stylelint-config-recommended-vue
```

| 包名                               | 作用                                         |
| ---------------------------------- | -------------------------------------------- |
| `stylelint`                        | Stylelint 核心                               |
| `stylelint-config-standard`        | 官方标准 CSS 规则集                          |
| `stylelint-config-recommended-vue` | 解析 `.vue` 中的 `<style>` 块               |

### 5.2 配置文件 `stylelint.config.js`（启用时新建于 `apps/web/`）

```js
export default {
  extends: ['stylelint-config-standard', 'stylelint-config-recommended-vue'],
}
```

### 5.3 运行（启用后）

```bash
cd apps/web
pnpm lint:style                              # 检查前端子包 css/vue 样式
pnpm exec stylelint "**/*.{css,vue}" --fix   # 自动修复
```

> 启用后同步：① `apps/web/package.json` 补 `"lint:style": "stylelint \"**/*.{css,vue}\""`；② 取消第 7.3 节 lefthook 中 stylelint 注释；③ 补回防卡死铁律里对 stylelint 的校验。

---

## 6. Commitlint（提交信息规范，前后端共用）

配置文件 `commitlint.config.mjs` 写在**仓库根目录**（跨端共用，使用 ESM 后缀），由 lefthook 在 `git commit` 时自动触发。

### 6.1 安装依赖

装在**仓库根** `package.json`（devDependency）：

```bash
pnpm add -D @commitlint/cli @commitlint/config-conventional
```

### 6.2 配置文件 `commitlint.config.mjs`

```js
export default {
  extends: ['@commitlint/config-conventional'],
}
```

### 6.3 提交信息格式（Conventional Commits）

```
<type>(<scope>): <subject>
```

常用 `type`：`feat`(新功能) / `fix`(修 bug) / `docs`(文档) / `style`(格式) / `refactor`(重构) / `perf`(性能) / `test`(测试) / `build`(构建) / `ci`(CI) / `chore`(杂项) / `revert`(回滚)。

合法示例：`feat: 新增用户登录页面`、`fix(cart): 修复购物车数量计算错误`。非法（被拦截）：`bad commit message`。

### 6.4 运行

```bash
echo "feat: xxx" | pnpm exec commitlint   # 手动校验一条提交信息
```

---

## 7. lefthook（Git 钩子自动化，前后端共用）

配置文件 `lefthook.yml` 写在**仓库根目录**（一份管全部子包，前端/后端都不单独引入），初始化命令也在根目录执行。lefthook 是用 Go 实现、以 npm 包形式安装（`pnpm add -D lefthook`）的前端侧工具，不是第 8 节的 Go 后端工具；它只调度检查、自身不检查代码。一份配置即可编排前端（Biome / ESLint）与后端（`gofmt` / `go vet`）的钩子。

### 7.1 安装依赖

```bash
pnpm add -D lefthook
```

### 7.2 初始化 lefthook

> lefthook 依赖 Git 仓库（本文档默认仓库已 `git init`）。

```bash
pnpm exec lefthook install   # 把 lefthook 挂到 .git/hooks
```

`package.json` 中建议添加 `prepare` 脚本，后续 `pnpm install` 会自动安装钩子：

```json
{ "scripts": { "prepare": "lefthook install" } }
```

### 7.3 配置文件 `lefthook.yml`（根目录）

```yaml
# lefthook.yml（仓库根）
pre-commit:
  parallel: true
  commands:
    biome:
      glob: "apps/web/**/*.{js,ts,vue,json,css}"
      skip_empty: true
      fail_text: "Biome 检查未通过，请根据上方输出修复后再提交。"
      # npm_config_ignore_scripts 避免 pnpm exec 触发根 prepare(lefthook install)，
      # 否则并行执行时多个进程同时 rename .pnpm/lock.yaml 会在 Windows 下 EPERM 竞态失败
      env:
        npm_config_ignore_scripts: "true"
      run: pnpm --filter web exec biome check --write .

    eslint-vue:
      glob: "apps/web/**/*.vue"
      skip_empty: true
      fail_text: "ESLint 检查未通过，请根据上方输出修复后再提交。"
      # 使用 bash -c 包裹以确保 Windows 下 pnpm bin 路径解析正常，避免 'eslint: command not found'
      env:
        npm_config_ignore_scripts: "true"
      run: pnpm --filter web exec eslint --fix .

    # stylelint: 暂未安装，后续有 CSS 规范需求时再启用（见第 5 节）
    #   glob: "apps/web/**/*.{vue,css,scss}"
    #   skip_empty: true
    #   env:
    #     npm_config_ignore_scripts: "true"
    #   run: pnpm --filter web exec stylelint --fix .

    typecheck:
      glob: "apps/web/**/*.{ts,vue}"
      skip_empty: true
      fail_text: "类型检查未通过（vue-tsc），请根据上方输出修复后再提交。"
      env:
        npm_config_ignore_scripts: "true"
      run: pnpm --filter web exec vue-tsc -b

    gofmt:
      glob: "apps/server/**/*.go"
      cwd: apps/server
      skip_empty: true
      fail_text: "gofmt 格式化未通过，已自动修复，请重新暂存后提交。"
      # 必须在 apps/server 目录内用 "." 扫描整个后端模块来格式化。
      # 切勿用 {staged_files}：它是相对仓库根的路径（如 apps/server/pkg/xxx.go），
      # 与 cwd: apps/server 叠加会变成 apps/server/apps/server/pkg/xxx.go，
      # gofmt 找不到文件会卡在等待 stdin 输入（表现为终端 ANSI 乱码卡死）。
      run: gofmt -w .

    govet:
      glob: "apps/server/**/*.go"
      skip_empty: true
      fail_text: "go vet 检查未通过，请根据上方输出修复后再提交。"
      # go vet 是包级检查（接受包路径而非文件列表），不传 {staged_files}，
      # 固定用 ./... 扫描整个后端模块
      run: bash -c "cd apps/server && go vet ./..."

    # markdownlint-cli2：纯 JS（Node）实现的 Markdown 格式化 + 规则修复工具，
    # 用 --fix 只修可自动修复的格式/规则问题、不阻塞提交。
    # 用 {staged_files} 限定只重写本次暂存的 .md（含中文路径），不动历史文档。
    # 选纯 JS 而非纯 Go(markdownfmt) 的根因：Go 二进制在 Windows 下依赖系统
    # "Beta UTF-8" 区域开关才能正确打开中文路径，否则 os.Open 中文路径直接失败；
    # Node 在 Windows 走 UTF-16 系统调用，天然支持中文路径，无此坑。
    mdformat:
      glob: "**/*.md"
      skip_empty: true
      fail_text: "Markdown 已自动格式化，请重新暂存（git add）后提交。"
      env:
        npm_config_ignore_scripts: "true"
      run: bash -c "pnpm exec markdownlint-cli2 --fix {staged_files}"

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
```

> 要点（配置已体现做法，这里只点关键坑）：
>
> - **前端别套 `bash -c`**：直接用 `pnpm --filter web exec ...`，多包一层会把参数吞掉报用法错误。
> - **前端传 `.` 不传 `{staged_files}`**：`{staged_files}` 是相对仓库根的路径，叠加 `--filter web` 的工作目录会变成 `apps/web/apps/web/...` 找不到文件。用 `.` 让工具自行扫描。
> - **Go 工具必带 `cwd: apps/server` 且传 `.`**：漏了 `cwd` 或用了 `{staged_files}` 都会路径翻倍，gofmt 找不到文件而卡死（终端 ANSI 乱码）。手动跑也先 `cd apps/server`。
> - **markdownlint-cli2 用 `{staged_files}` 即可**：纯 JS 工具，Windows 下天然支持中文路径不卡死，精准只改暂存的 `.md`；改完需重新 `git add`。
> - **`npm_config_ignore_scripts: "true"` 别删**：前端命令并行时，`pnpm exec` 会并发触发 `prepare` 重挂钩子，Windows 下对 `.pnpm/lock.yaml` 并发 rename 报 `EPERM`。加这行跳过 `prepare` 即消除竞态（`env` 与 `run` 同级，Go 工具无需）。
> - **commitlint 在根目录 `pnpm exec`**，Biome / ESLint 职责不重叠（见第 3、4 节），Stylelint 暂未启用。

---

## 8. Go 后端工程化（仅 Go 后端）

后端使用 Go，位于 **`apps/server/`**（与前端 `apps/web/` 同为独立子包，从项目初始化即建好）。Go 自带 `gofmt` / `go vet`，另通过 `golangci-lint` 做聚合式检查。各工具的配置与执行目录分属不同层级，请注意区分：

- **gofmt / go vet**：Go 自带，**无配置文件**；执行时必须先 `cd apps/server` 进入后端包目录，再按包路径运行（不要在仓库根目录直接跑，否则会误扫不到 `.go` 或扫到错误位置）。
- **golangci-lint**：配置文件 `.golangci.yml` 放在 `apps/server/` 下（见 8.2 节），执行同样在 `apps/server/` 内。
- **lefthook 钩子**：在仓库根级 `lefthook.yml` 统一声明一次（见第 7 节），无需在 `apps/server` 单独引入。

### 8.1 目录结构

```bash
apps/
  server/          # Go 后端（独立子包）
    cmd/           # 程序入口
    internal/      # 内部业务代码
    go.mod         # 模块定义（module github.com/liwook/go-vue-selection）
    go.sum
    main.go
```

> `.go` 文件由 lefthook 的 `gofmt` / `go vet` 钩子自动格式化与检查（见第 7.3 节）。

**最小可运行 `main.go`**（放在 `apps/server/main.go`，确保 `go build ./...` / lefthook 的 `go vet` 能跑通，不报空包）：

```go
package main

import "fmt"

func main() {
 // 占位示例：后端入口。后续把业务初始化逻辑放这里。
 fmt.Println("server started")
}
```

### 8.2 golangci-lint（聚合式 lint，v2）

> 📌 **写在哪 / 跑在哪**：配置文件 `.golangci.yml` 写在 **`apps/server/` 目录内**（不在仓库根）；运行命令也必须在 **`apps/server/` 目录内**执行（先 `cd apps/server` 再 `golangci-lint run ./...`）。

**安装（本机环境，非仓库内容，随时可做）：**

```bash
go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest
```

**配置 `.golangci.yml`（写在 `apps/server/`）：**

 使用v2 对应的配置文件

```yaml
# apps/server/.golangci.yml（v2 格式）
version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - gofmt
    - govet
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
```

运行：

```bash
cd apps/server
golangci-lint run ./...
```

---

### 8.3 测试与竞态检测（`-race`）

后端集成测试位于 `apps/server/tests/`，通过进程内 `httptest.Server` 串行发起 HTTP 请求，用例之间无并发，包级 `apiClient` 在 `TestMain` 中初始化后只读——**数据库隔离由 PostgreSQL 保证（非 Go 内存共享），因此日常本地回归不需要带 `-race`**：

```bash
cd apps/server
go test -tags=integration ./tests/...
```

`-race` 的意义是"体检"被测的服务器代码（handler / middleware / 全局组件）在并发请求下是否隐藏数据竞争。建议放到 **CI 定时任务或发布前检查**中跑一遍（运行成本仅慢几倍）：

```bash
cd apps/server
go test -tags=integration -race ./tests/...
```

前提：必须在 `apps/server` 目录下，且能连上 `vue_admin_test` 库、存在 `./etc/config.yaml`（由 `TestMain` 的 `config.Init("../etc/config.yaml")` 加载）。若后续编写并发 / 压测类用例（使用 `t.Parallel()` 或多个 goroutine 复用 `apiClient`），则必须默认带 `-race`。

---

## 8.4 markdownlint-cli2（Markdown 格式化，前后端共用）

`markdownlint-cli2` 是纯 JS 实现的 Markdown 格式化 + 修复工具，装在根 `package.json`，由 lefthook 在 `git commit` 时统一调度（见第 7.3 节），前后端共用。`--fix` 只修可自动格式化的项、不阻塞提交。

### 8.4.1 安装依赖

装在**仓库根** `package.json`（devDependency，`markdownlint` 是规则引擎、`markdownlint-cli2` 是 CLI 包装，二者配套）：

```bash
pnpm add -Dw markdownlint markdownlint-cli2
```

### 8.4.2 配置文件 `.markdownlint-cli2.jsonc`（仓库根）

```jsonc
{
  "config": {
    "default": true,
    "MD013": false, // 行宽：文档含长 URL/表格，不强制折行
    "MD033": false, // 允许内联 HTML（如语雀占位、原始标签）
    "MD041": false  // 允许文件无一级标题
  },
  "ignores": ["node_modules/**", "**/dist/**", "**/coverage/**"]
}
```

### 8.4.3 运行

```bash
pnpm format:md                          # 全量自动修复（= markdownlint-cli2 --fix "**/*.md"）
pnpm exec markdownlint-cli2 "**/*.md"   # 仅检查不修改，输出违规项
```

由 lefthook 在 `git commit` 时自动触发：仅对本次暂存的 `.md` 执行 `--fix`，改写后需重新 `git add` 再提交（见第 7.3 节 `fail_text`）。

> 注：语雀占位符 `@image:xxx.png` 非标准 Markdown，按 MD033（已关闭）放行不报错；转标准语法 `![alt](./path.png)` 需手工替换。当前仓库无此语法，故不处理。

---

## 9.0 从零初始化 monorepo 骨架（首次必读）

> 本仓库是 pnpm workspace monorepo：**根 `package.json` 只放跨端共用字段**（如 `prepare` 钩子、包管理器锁定），前端业务脚本放在 `apps/web/package.json` 中。具体写法见下方 9.1（根）与 9.2（前端子包）。

> ⚠️ **正确顺序铁律**：**先建好仓库根骨架（pnpm workspace + 根 `package.json` + `pnpm-workspace.yaml`），再在 `apps/web/` 目录内执行 `pnpm create vite`**。绝不能先裸跑 create vite，否则生成的前端文件会脱离 workspace 管理、散落到根目录。

#### 步骤 1：建根骨架（仓库根目录）

> 文档默认：仓库已 `git init`，根目录已有 `README.md` 与 `.gitignore`；本步骤只在根目录**新增**工程化文件，请勿改动这两个已有文件。

在**仓库根目录**创建以下文件（内容见对应章节，直接复制即可）：

| 文件 | 内容来源 |
| ---- | -------- |
| `pnpm-workspace.yaml` | 见 9.1 末尾（**含 `allowBuilds.lefthook: true`，必须放行，否则 pnpm v10+ 下 lefthook 钩子装不上**） |
| `package.json`（根） | 直接写**完整版**（见 9.1，只放跨端共用字段，不装前端依赖） |
| `.npmrc` | 见 2.1（`engine-strict=true` + `auto-install-peers=true`，强制使用 pnpm 并自动安装 peer 依赖） |
| `.gitattributes` | 见 9.1（`* text=auto eol=lf`，统一换行符，避免 Windows 下 Biome 的 `lineEnding: lf` 反复改动文件） |
| `lefthook.yml` | 见 7.3 |
| `commitlint.config.mjs` | 见 6.2 |

```bash
# 在仓库根目录执行
mkdir apps
```

> 根 `package.json` 的 `prepare: "lefthook install"` 会在步骤 6 的 `pnpm install` 时自动跑，这里先把 `lefthook.yml` 放好即可。

#### 步骤 2：在 `apps/web/` 内用 Vite 脚手架生成前端子包

> 末尾的 `.` 表示「在当前目录 `apps/web/` 生成」，避免再套一层 `apps/web/web/` 子目录。模板选 **`vue-ts`**（Vue 3 + TypeScript）。

```bash
mkdir apps/web
cd apps/web
pnpm create vite@latest . --template vue-ts
```

执行时若提示目录非空/冲突，选「忽略文件并继续」即可（目录此时为空，正常生成）。生成后会得到 `package.json`、`vite.config.ts`、`tsconfig*.json`、`src/` 等。

#### 步骤 3：用 9.2 脚本覆盖前端 `package.json` 的 `scripts`

`pnpm create vite` 生成的 `apps/web/package.json` 中，**依赖（dependencies/devDependencies）原样保留，仅用 9.2 节的 `scripts` 覆盖**（二者拼接，不要二选一）。

> 📌 根 `package.json` 已在步骤 1 写好，本步只改前端的 `scripts`，**无需再碰根 `package.json`**。改完 **继续步骤 4**。

#### 步骤 4：补前端工具依赖 + 配置文件（仍在 `apps/web/` 内）

上一步只改了 `scripts`，还**必须**把 Biome / ESLint 的依赖装好，并把它们的配置文件放进 `apps/web/`（与 `package.json` 同级），否则 `pnpm lint` / `format` / `lint:vue` 会直接报错。Stylelint 暂未安装，无需在此步处理（后续启用时见第 5 节）。

**(a) 安装依赖**（详见 4.1 / 3.1 / 5.1 节）：

> ⚠️ **必须在仓库根目录执行，用 `--filter web` 指定前端子包，不要进 `apps/web/` 里直接 `pnpm add`。**
> 原因：根 `package.json` 的 `prepare: "lefthook install"` 会在任何 `pnpm install`/`pnpm add` 后触发；而 lefthook 还没装好时跑它就会报「不是内部或外部命令」。在**根目录**装，可让 lefthook 与前端依赖在同一次 workspace 安装里一起就绪，`prepare` 不会再报错。

```bash
# 回到仓库根目录（若已在 apps/web/，先 cd ../..）
cd <仓库根目录>

# 先装根级依赖（含 lefthook / commitlint），让 prepare 钩子有东西可挂
pnpm add -D lefthook @commitlint/cli @commitlint/config-conventional

# 再用 --filter web 把前端工具依赖装进 apps/web 子包
# Biome 主力（lint + 格式化）
pnpm add -D --filter web @biomejs/biome
# ESLint 仅 .vue 模板的最小集（含内层 TS 解析器 @typescript-eslint/parser）
pnpm add -D --filter web eslint eslint-plugin-vue vue-eslint-parser @typescript-eslint/parser globals
# Stylelint 暂未安装，后续有 CSS 规范需求时再执行：
# pnpm add -D --filter web stylelint stylelint-config-standard stylelint-config-recommended-vue
```

> 若你已不小心在子包里跑过 `pnpm add` 并看到 `prepare` 报错，无需回滚——依赖其实已装入 store，按上面在根目录补跑一遍即可，最后步骤 6 的 `pnpm install` 会让 `prepare` 成功收尾。
> ⚠️ **前提**：本段能成功收尾的关键，是**步骤 1 已把 `allowBuilds.lefthook: true` 写进 `pnpm-workspace.yaml`**（见 9.1）。若漏了这步，`pnpm install` 仍会报 `[ERR_PNPM_IGNORED_BUILDS]`，`prepare` 钩子异常——先回步骤 1 补上 `allowBuilds` 再继续。

**(b) 创建配置文件**：在 `apps/web/` 下新建 `biome.json`（主力 lint + 格式化）与 `eslint.config.js`（仅 `.vue` 模板）共 2 个文件。**配置内容见下方对应章节，直接复制即可，此处不重复贴出**：

- `biome.json` → 完整配置见 **第 3.3 节**（含 `overrides` 为何关闭 `useImportType` / `noUnused*` 的注释，复制时连同注释一起带走）。
- `eslint.config.js` → 完整配置见 **第 4.2 节**（含内层 `@typescript-eslint/parser` 注释）。

> ⚠️ **首次提交前先验证**：`pre-commit` 钩子会调用 `biome`/`eslint`，若步骤 4 漏装工具或漏放配置文件，`git commit` 会失败、本次搭建的文件未固化进 git。提交前先确认两者可用：

> ```bash
> pnpm --filter web exec biome --version
> pnpm --filter web exec eslint --version
> ```
>
> 都输出版本号再 `git add -A && git commit -m "chore: init 工程化脚手架"`；不通过则回步骤 4 重做。

#### 步骤 5：建 Go 后端子包（最小可运行）

> 以下命令**从仓库根目录出发**执行（`cd apps/server` 后再初始化模块，与步骤 1/2/6 的目录约定一致）。

```bash
mkdir -p apps/server
cd apps/server
go mod init github.com/liwook/go-vue-selection     # 在 apps/server/ 目录内初始化，module path 用仓库根 URL

# 最小可运行 main.go
cat > main.go <<'EOF'
package main

import "fmt"

func main() {
 fmt.Println("server started")
}
EOF
```

`.golangci.yml` 的完整配置（v2 格式）见 **第 8.2 节**，直接复制其内容写入 `apps/server/.golangci.yml` 即可。

> 📌 **module 命名约定**：在 `apps/server/` 执行 `go mod init github.com/liwook/go-vue-selection`（用仓库根 URL，不加子路径段）；导入时以「根 module + 包相对目录」（如 `/cmd`、`/internal/...`）引用即可。

#### 步骤 6：回到根目录，统一收尾安装并挂载钩子

> 仓库已 `git init`（默认前提），无需再初始化。
> 由于步骤 4 已在根目录用 `--filter web` 把前端依赖和根级 `lefthook`/`commitlint` 一起装好，本步只需在**根目录**跑一次 `pnpm install` 让 workspace 对齐，并确认钩子挂载。

```bash
cd <仓库根目录>                  # 确保位于仓库根（不是 apps/web）
pnpm install                    # 按 workspace 对齐依赖；prepare 脚本此时会成功跑 lefthook install
pnpm exec lefthook install      # 显式再确认一次 Git 钩子已挂载（prepare 通常已自动完成）
```

> 到此，**前后端环境已完整就绪**。按以下命令逐项验证（无报错即代表搭建成功）：

```bash
# 前端（仓库根执行）
pnpm --filter web lint            # Biome 检查无错误
pnpm --filter web lint:vue        # ESLint 仅查 .vue 模板无错误
pnpm --filter web format          # Biome 格式化
pnpm --filter web build           # vue-tsc 类型检查 + vite 构建通过
echo "feat: test" | pnpm exec commitlint   # 提交信息校验通过

# 后端
cd apps/server
go build ./...        # 编译通过
go vet ./...          # 静态检查通过
cd ../..
```

---

### 9.1 仓库根 `package.json`（workspace 根）

> 📌 **根 `package.json` 在【步骤 1】一次性写完整**，不经历「空壳 → 复制删减」流程。它只放跨端共用字段（钩子 + 包管理器锁定），**不装任何前端依赖**（前端依赖在 `apps/web/package.json`）。

**完整内容（步骤 1 直接写入）**：

```json
{
  "name": "your-repo-name",
  "scripts": {
    "prepare": "lefthook install",
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "lint:vue": "pnpm --filter web lint:vue",
    "format": "pnpm --filter web format"
  },
  "packageManager": "pnpm@<你安装的版本>",   // 填 pnpm -v 的版本；若不确定/不想锁定，直接删除此行即可
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
```

同时需要 `pnpm-workspace.yaml` 声明子包，并**放行 lefthook 的构建脚本**（pnpm v10+ 默认拦截依赖的构建脚本，不放行会导致 `lefthook install` 与 `prepare` 钩子异常）：

```yaml
packages:
  - "apps/*"
# pnpm v10+ 默认禁止依赖自动运行构建脚本；lefthook 是 Go 二进制，
# 其 postinstall 负责同步 Git 钩子，必须显式放行，否则 prepare 钩子会失败。
allowBuilds:
  lefthook: true
# catalog：在 workspace 级别统一管理依赖版本，避免子包版本漂移（pnpm 9+ 特性）。
# 子包里用 "catalog:" 引用，见 9.2。
catalog:
  # 仅锁定跨项目通用的工程化工具版本，框架依赖（Vue/Vite等）由官方脚手架管理，避免非预期版本覆盖
  "@biomejs/biome": ^2.5.7
```

> ⚠️ **Catalog 仅锁工程化工具**：`@biomejs/biome`、lefthook 等基建依赖可纳入 catalog；Vue / Vite / TS 等由 `create vite` 管理的框架依赖**严禁纳入**，否则首次 `pnpm install` 会重新解析依赖树导致版本漂移。升级框架请先升 `create vite` 模板。

> ⚠️ **`allowBuilds` 必须在顶层**：pnpm v10+ 默认拦截依赖构建脚本（如 lefthook 的 postinstall），不在此放行则 `prepare` 报 `[ERR_PNPM_IGNORED_BUILDS]`，Git 钩子挂不上。

根目录还需 `.gitattributes`，确保 Biome 的 `lineEnding: lf` 在跨平台一致（否则 clone 后 CRLF 会让 pre-commit 反复改文件）：

```bash
cat > .gitattributes <<'EOF'
* text=auto eol=lf
EOF
```

根 `package.json` 字段说明：

- 强制 pnpm 由 `.npmrc` 的 `engine-strict=true` + `engines.pnpm` 保证，无需 `preinstall`（见第 2 节）。
- `prepare`：每次 `pnpm install` 后挂载 lefthook 钩子。
- `dev` / `build` / `lint` / `lint:vue` / `format`：根目录代理脚本，转发到 `apps/web`（逻辑与依赖仍在 9.2）。
- Go 后端的 `gofmt` / `go vet` 由 lefthook 钩子自动执行（见第 7、8 节），不在此处。

### 9.2 前端子包 `apps/web/package.json`

前端专属依赖与脚本写在 `apps/web/package.json`：

```json
{
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "eslint": "^9.0.0",
    "eslint-plugin-vue": "^10.0.0",
    "vue-eslint-parser": "^10.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "globals": "^15.0.0"
  },
  "scripts": {
    "dev": "vite --open",
    "build": "vue-tsc -b && vite build",
    "build:test": "vue-tsc -b && vite build --mode test",
    "preview": "vite preview",
    "lint": "biome check .",
    "lint:vue": "eslint .",
    "format": "biome check --write ."
  }
}
```

脚本说明：

- `dev`：`vite --open` 中的 `--open` 会在开发服务器启动后**自动打开默认浏览器**，省去手动访问 `localhost` 地址。
- `build`：先 `vue-tsc -b` 做类型检查，再 `vite build` 构建产物到 `dist`（生产环境，加载 `.env.production`）。`vue-tsc -b` 的 `-b` 是 Vue 3.4+ / vue-tsc 2.x 的 build mode 写法，请确保脚手架模板为 Vue 3.4+（2024 年起的 `create-vue` / `pnpm create vite` 默认满足）。
- `build:test`：以 `test` 模式构建测试环境产物（加载 `.env.test`）。结构与 `build` 一致，仅 `--mode test` 不同。
- `preview`：本地预览已构建的产物。
- `lint`：Biome 检查（`biome check .`，见第 3 节，前端 lint + 格式化主力）。
- `lint:vue`：ESLint 仅查 `.vue` 模板（`eslint .`，见第 4 节）。
- `format`：`biome check --write .`（lint 修复 + 格式化）。Stylelint 暂未启用，故 `lint:style` 未列入默认 `scripts`（后续启用见第 5 节）。

> 注：框架依赖由 `create vite` 锁版本、已移出 catalog（见 9.1 铁律）；列出的 ESLint 依赖是用于对照步骤 4(a) 的最小集，其中 `@typescript-eslint/parser` 的作用见第 4 节。

### 9.3 从根目录调用前端脚本

在仓库根目录调用前端脚本有两种等价写法：

- **简写**（根 `package.json` 已配代理脚本，推荐）：`pnpm dev` / `pnpm build` / `pnpm lint` / `pnpm lint:vue` / `pnpm format`
- **显式指定子包**：`pnpm --filter web dev` 等，无需手动 `cd`

```bash
pnpm dev            # 等价 pnpm --filter web dev，启动前端开发服务器
pnpm build          # 前端构建
pnpm lint           # Biome 检查
pnpm lint:vue       # ESLint 仅查 .vue 模板
pnpm format         # Biome 格式化
```
---
